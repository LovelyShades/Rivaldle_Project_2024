'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var options = require('./options.js');
var label = require('./label.js');
var toChinese = require('./to-chinese.js');

function numberToChineseOrdinalWithArabic(num, options$1) {
  return label.getLabels(options.normalizeOptions(options$1)).ordinal + num.toString(10);
}
function numberToChineseOrdinal(num, options$1) {
  const opts = options.normalizeOptions(options$1);
  return label.getLabels(opts).ordinal + toChinese.numberToChinese(num, opts);
}

exports.numberToChineseOrdinal = numberToChineseOrdinal;
exports.numberToChineseOrdinalWithArabic = numberToChineseOrdinalWithArabic;
//# sourceMappingURL=to-ordinal.js.map
